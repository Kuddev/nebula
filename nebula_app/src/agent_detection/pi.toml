# Screen-chrome evidence for this CLI (idle / working / blocked).
id = "pi"

[[rules]]
id = "working_literal"
state = "working"
priority = 300
region = "bottom_non_empty_lines(8)"
contains = ["Working..."]
