interface:
  display_name: "Nebula Runtime"
  short_description: "Observe and control Nebula agent panes"
  default_prompt: "Use $nebula-runtime to inspect Nebula agent panes and continue the task that needs attention."
