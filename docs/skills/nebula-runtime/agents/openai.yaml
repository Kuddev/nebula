interface:
  display_name: "Nebula Runtime"
  short_description: "Split panes and control Codex/Claude in Nebula"
  default_prompt: "Use $nebula-runtime to control the live Nebula layout, start or prompt coding agents, wait for state changes, and verify terminal output."
