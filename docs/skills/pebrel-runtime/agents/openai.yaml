interface:
  display_name: "Pebrel Runtime"
  short_description: "Control Pebrel panes and delegate tasks to terminal agents"
  default_prompt: "Use $pebrel-runtime to inspect my terminal workspace, resolve the intended pane or agent, and carry out the requested actions through the Runtime API."
